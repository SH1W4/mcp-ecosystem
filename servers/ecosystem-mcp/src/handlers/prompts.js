import { logger } from '../utils/logger.js';

export async function setupPromptHandlers(server) {
  // Prompts serão implementados em versões futuras
  logger.debug('Prompt handlers placeholder configured');
}


import { logger } from '../utils/logger.js';

export async function setupResourceHandlers(server) {
  // Recursos serão implementados em versões futuras
  logger.debug('Resource handlers placeholder configured');
}

